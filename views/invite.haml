.invite
  %p
    Please give me access to the
    %br
    %a{ href: "#{team_url}", title: "#{team_name}", target: "_blank" }
      %b= team_name
    = "#Slack team!"

  %p
    %small
      %i
        (And don't use my email for other purposes.)


  %form.form-inline(action="/invite" method="post")
    .form-group
      .field
        %label(for="first_name")
          Name
        %input.form-control(type="text" name="first_name" id="first_name")
      .field
        %label(for="email")
          Email
        %input.form-control(type="email" name="email" id="email")
    .form-group
      .field
        %input.btn.btn-lg.btn-success.btn-default(type="submit")


.container-fluid
  .row#hideMe
    - if defined? success
      .alert.alert-success
        = success
    - if defined? failure
      .alert.alert-danger
        = failure
