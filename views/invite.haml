.invite
  %p= "Please give me access to the <b>#{slack_channel_name} #slack</b> group."
  %p <i>(And don't use my email for other purposes.)</i>


  %form(action="/invite" method="post")
    .field
      .label Name:
      %input(type="text" name="first_name")
    .field
      .label Email:
      %input(type="email" name="email")
    .field
      %input(type="submit")


- if defined? success
  .success
    = success


- if defined? failure
  .failure
    = failure
